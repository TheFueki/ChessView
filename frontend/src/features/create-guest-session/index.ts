/**
 * Feature: create-guest-session
 *
 * Allows a user to enter a display name and obtain a temporary session
 * without full registration. Sets user store with guest identity.
 *
 * FSD layer: features
 * May import: entities, shared
 *
 * TODO: Implement GuestForm component
 * TODO: Call POST /api/identity/guest (or register with generated creds)
 * TODO: Store tokens in user entity store
 */

export {};
